meta(charset="UTF-8")
meta(name="viewport", content="width=device-width,initial-scale=1")
title Sales Entry Form
link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel="stylesheet")
link(href="css/stock.css", rel="stylesheet")
nav.navbar.navbar-expand-lg
  .container-fluid
    .navbar-brand.d-flex.flex-column.text-white
      span MWF Ltd
    button.navbar-toggler(type="button", data-bs-toggle="collapse", data-bs-target="#navbarNav")
      span.navbar-toggler-icon
    .collapse.navbar-collapse#navbarNav
      ul.navbar-nav.ms-auto
        li.nav-item
          a.nav-link.text-white(href="/signup") signup
        li.nav-item
          a.nav-link.text-white(href="/dashboard") dashboard
        li.nav-item
          a.nav-link.text-white(href="/stock") Stock
        li.nav-item
          a.nav-link.active.text-white(href="Addsale") Sales
        li.nav-item
          a.nav-link.text-white(href="/getusers") users
        li.nav-item
          a.nav-link.text-white(href="/logout") Logout
.container.my-4
  h2 Record New Sale
  p.text-muted.
    
    Fill in the customer details and finalize the sale.
    
  h4 Sales Entry Form
  form#saleForm(action="/Addsale", method="post")
    .mb-3
      label.form-label Customer Name
      input#customerName.form-control(type="text", name="customername", placeholder="Enter customer name", required)
    .mb-3
      label.form-label(for="product type") Product Type
      select#producttype.form-select(name="producttype", required)
        option(value="") --Select Product Type--
        option(value="Wood") Wood
        option(value="Furniture") Furniture
    .mb-3
      label.form-label(for="product") Product
      input#product.form-control(type="text", name="product", placeholder="Product", required)
    .mb-3
      label.form-label(for="quantity") Quantity
      input.form-control#quantity(type="number", name="quantity", placeholder="Enter quantity")
    .mb-3
      label.form-label(for="sales agent") Sales Agent
      input#salesAgent.form-control(type="text", name="salesAgent", placeholder="Attendant name", required)
    .mb-3.form-check
      label.form-check-label(for="transportfee") Add 5% Transport Fee
      input.form-check-input#transportCheck(type="checkbox", name="transportCheck")
    .mb-3
      label.form-label(for="paymentMethod") Payment Method
      select#paymentMethod.form-select(name="paymentMethod", required)
        option(value="") --Select Payment Method--
        option(value="Cash") Cash
        option(value="Cheque") Cheque
        option(value="Bank Overdraft") Bank Overdraft
    .mb-3
      label.form-label(for="date") Date
      input#date.form-control(type="date", name="paymentdate", required)
    //
       Final Total
              <div class="alert alert-info">
                <strong>Final Total:</strong> UGX <span id="final-total">0</span>
              </div> 
    button.btn.btn-outline-dark(type="submit") sumbit
.container.my-5
  h3 Sales Records
  table.table.table-striped
    thead
      tr
        th Customer
        th Product Type
        th Product Name
        th Quantity
        th Agent
        th Date
        th Payment
        th Total (UGX)
    tbody#sales-records
  h5.mt-3
    | Final Total: UGX 
    span#rFinalTotal
script(src="public/js/sales.js")